/// <reference types="vite/client" />

// Types `import.meta.env` (DEV/PROD/etc.) for Vite. The dashboard ships no `.task`
// model assets — gesture/MediaPipe support was removed to keep it strictly camera-free.
