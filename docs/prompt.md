## §1 · Project Identity

- **Title:** Jarvis Reactor Core — Marvel-Grade UHD Cinematic Interactive Baseline (V4 / Marvel Build)
- **Scope:** Construct a new Vite-bundled, WebGL-driven, audio-reactive HUD baseline at `prototypes/jarvis-reactor-cinematic-marvel.html` (loader) + `js/src/reactor-cinematic-marvel.ts` (entry) that merges the strongest visual elements of the existing prototypes (`jarvis-uhd-cinematic-v2.html`, `jarvis-drkdna-baseline.html`, `jarvis-reactor-target.html`, `compare.html`) with the dependency stack locked in the immediately-preceding step (Three.js r170+, pmndrs/postprocessing 6.39+, three-particles 2.15+, GSAP 3.12+ MotionPath, Anime.js 4+, Meyda 5+, locked via `uv.lock` + `js/package-lock.json`).
- **Target Platform:** Chromium ≥ 120, Safari ≥ 17, Firefox ≥ 121; UHD/4K rendering target (3840×2160) at 60 Hz+; M-series Mac primary validation rig.
- **Stack:** TypeScript 5+, Three.js r170+, `pmndrs/postprocessing` 6.39+ (`EffectComposer`/`RenderPass`/`EffectPass`), `@react-three/drei` helpers (volumetric `SpotLight`, `useDepthBuffer`) imported as plain Three.js helpers, `three-particles` 2.15+ for GPU-instanced sparks/ribbons, `gsap` 3.12+ with `MotionPathPlugin`, `animejs` 4+ for SVG path stroke draw, `meyda` 5+ for audio FFT extraction, ported GLSL shaders (`Erkaman/glsl-godrays`, anamorphic-lens-flare GLSL, GShade chromatic aberration), inline SVG for dendritic neural pathways, Vite 6+ build pipeline (`js/vite.config.js`), Python validation harness via `uv` (`playwright` 1.50+, `pytest` 9+, `Pillow` 11+, `numpy` 2.4+, `ffmpeg-python` 0.2+).
- **Mission:** Ship a Marvel-grade JARVIS reactor core — counter-rotating PBR C-shape rings, real WebGL bloom + chromatic aberration + anamorphic lens flare + god rays, GPU-instanced charge particles, audio-reactive cadence — fully leveraging the existing `prototypes/` design language and the previously-locked dependency stack.
- **Reference Assets:**
  - `/Users/vic/claude/General-Work/jarvis/jarvis-build/prototypes/jarvis-uhd-cinematic-v2.html` — primary parent (V3 in-place implementation; carry forward neural pathways, telemetry layout, trigger panel, MOOD/BPM engine)
  - `/Users/vic/claude/General-Work/jarvis/jarvis-build/prototypes/compare.html` — comparator harness (additive chip registration target)
  - `/Users/vic/claude/General-Work/jarvis/jarvis-build/prototypes/jarvis-drkdna-baseline.html` — DRKDNA baseline (frame proportions reference)
  - `/Users/vic/claude/General-Work/jarvis/jarvis-build/prototypes/jarvis-reactor-target.html` — explicit target design (geometry source of truth)
  - `/Users/vic/claude/General-Work/jarvis/jarvis-build/prototypes/refs/drkdna-jarvis-ui-frame-12.png`, `drkdna-jarvis-ui-frame-24.png` — extracted reference frames
  - `/Users/vic/Downloads/JARVIS.gif` — canonical reactor-core baseline source
  - `https://www.vecteezy.com/video/1625732-white-hud-circle-user-interface`
  - `https://www.vecteezy.com/video/1624472-futuristic-user-interface-hud`
  - `https://www.vecteezy.com/video/2018275-hud-circle-hologram-user-interface-technology`
  - `https://www.vecteezy.com/video/2015841-elements-hud-circle-hologram-user-interface-technology`
  - `https://github.com/muskankhedia/Jarvis-Desktop` (LGPL-2.1 — visual reference only; license gate, NO vendoring)
  - Locked dependency manifests from prior step: `pyproject.toml`, `uv.lock`, `js/package.json`, `js/package-lock.json`

## §2 · Requirements

- **R-1** Leverage existing prototypes — additive baseline build
  - **R-1.1** Inventory all `prototypes/*.html` files via `Glob` and document them in the parity report appendix
  - **R-1.2** Identify `jarvis-uhd-cinematic-v2.html` as the primary parent and explicitly enumerate the features carried forward (MOOD/BPM engine, neural pathways, telemetry layout, trigger panel, phase state machine, JARVIS public API)
  - **R-1.3** Create the new file `prototypes/jarvis-reactor-cinematic-marvel.html` as the HTML loader, served at `http://localhost:8899/jarvis-reactor-cinematic-marvel.html`
  - **R-1.4** All existing prototypes (v2, compare.html, drkdna-baseline, reactor-target) continue to load HTTP 200 with zero new console errors after the change
- **R-2** Real WebGL post-processing pipeline (replace V3 Canvas-2D approximations)
  - **R-2.1** `EffectComposer` instantiated with `RenderPass` + `EffectPass` from `pmndrs/postprocessing`
  - **R-2.2** Real `BloomEffect` with HDR tone mapping enabled
  - **R-2.3** Real `ChromaticAberrationEffect` registered in the composer pass list
  - **R-2.4** Anamorphic lens flare via ported GLSL fragment shader (from `Anamorphic Lens Flare` Godot shader, MIT) integrated as a `ShaderPass`
  - **R-2.5** Volumetric god rays via `Erkaman/glsl-godrays` ported into a custom `Effect` that contributes ≥ 1000 non-zero pixels in a sampled frame
- **R-3** Three.js mechanical wheel + C-shape concentric rings (3D, PBR-shaded)
  - **R-3.1** Three.js scene contains ≥ 3 named C-shape ring meshes (`coreRingInner`, `coreRingMid`, `coreRingOuter`) with the existing V3 cut/double-ring spec preserved (cuts `30°-40°`, `180°-190°`, `210°-220°`; double-ring spans `40°-170°`, `230°-350°`)
  - **R-3.2** Adjacent ring layers rotate in opposite directions with non-linear easing (per-layer angular velocity variance ≥ 15 % within one revolution)
  - **R-3.3** `drei` volumetric `SpotLight` with `volumetric=true` illuminates the rings from above the scene
  - **R-3.4** Rings use `MeshPhysicalMaterial` with `metalness ≥ 0.7`, `roughness ≤ 0.3`, and an `emissive` channel modulated by audio bass band (R-6.2)
- **R-4** GPU-instanced particle systems (sparks, ambient motes)
  - **R-4.1** `three-particles` GPU-instanced renderer wired for charge-event spark bursts (carry forward from V3 `JT.trigger('charge')`)
  - **R-4.2** Ambient motes (≥ 50 simultaneous particles) drift around the reactor during NOMINAL phase
  - **R-4.3** Trigger panel (`JT.trigger('cpu' | 'gpu' | 'thermal' | 'power' | 'charge' | 'memory' | 'network' | 'disk')`) preserved verbatim from V3 and fires the new GPU spark cascade plus the new bloom envelope
- **R-5** SVG dendritic neural pathway layer (carry forward + audio-reactive enhancement)
  - **R-5.1** Inline SVG layer with 4 trunks (L/R/T/B clusters) and ≥ 20 leaves per V3 spec preserved
  - **R-5.2** Each leaf carries `<animateMotion>` with `dur ≥ 2s` (R-4.2 of prior reconstruction preserved)
  - **R-5.3** Endpoint plate text/background combinations meet WCAG AA contrast ratio ≥ 4.5 : 1
  - **R-5.4** Reactor central disk `(CX, CY, R*0.55)` contains zero text-bearing element bounding boxes (HC-2 from V3 preserved verbatim)
- **R-6** Audio-reactive coupling via Meyda
  - **R-6.1** `Meyda.createMeydaAnalyzer` instantiated and bound to the existing `MOOD.bpm` clock
  - **R-6.2** Bass FFT band (≤ 250 Hz) drives the bloom envelope (`bloomMul` correlation Pearson ≥ 0.5 over 5 s capture)
  - **R-6.3** Mid + treble FFT bands (250 Hz – 16 kHz) drive ring rotation cadence (`ringSpeedMul` correlation Pearson ≥ 0.5 over 5 s capture)
  - **R-6.4** Microphone-permission-denied path triggers a silent fallback (constant 0 audio input) with zero console errors
- **R-7** Choreographed motion via GSAP MotionPath + Anime.js
  - **R-7.1** GSAP `MotionPathPlugin` choreographs the BOOT, LOCK, SHUTDOWN phase camera/element sequences
  - **R-7.2** Anime.js (or Motion One) drives SVG path stroke-dasharray draw animation on BOOT phase entry
- **R-8** Marvel-grade cinematic camera + timing
  - **R-8.1** `PerspectiveCamera` with subtle parallax + dolly-zoom on phase transitions wired to the existing `setPhase()` API
  - **R-8.2** All cadence references the single `MOOD.bpm` clock; no parallel `setInterval`/`requestAnimationFrame` driver outside the central frame loop
  - **R-8.3** Sustained ≥ 58 FPS at 1920×1080 measured via Chrome DevTools performance trace over 30 s
- **R-9** Build pipeline using locked uv + npm + Vite from prior step
  - **R-9.1** `uv sync --frozen` (from prior step's `pyproject.toml` + `uv.lock`) exits 0 with zero drift
  - **R-9.2** `npm ci --prefix js` (from prior step's `js/package.json` + `js/package-lock.json`) exits 0 with zero drift
  - **R-9.3** New entry point at `js/src/reactor-cinematic-marvel.ts` imports only from locked deps (no inline `import` URLs)
  - **R-9.4** `npm run build --prefix js` produces `prototypes/dist/reactor-cinematic-marvel.js` (and any chunk + map siblings)
  - **R-9.5** `prototypes/jarvis-reactor-cinematic-marvel.html` loads the bundle via a relative `<script type="module" src="./dist/reactor-cinematic-marvel.js">` — zero 404s in the network panel
- **R-10** `compare.html` integration (additive only)
  - **R-10.1** A new chip is registered in `compare.html`'s chip bar that swaps the comparator iframe to the marvel baseline
  - **R-10.2** Centre crosshairs intersect the new baseline reactor centre `(CX, CY)` within ±2 px at 1920×1080
- **R-11** Visual fidelity to JARVIS.gif + 4 Vecteezy references
  - **R-11.1** Side-by-side full-page captures saved at 1920×1080 AND 3840×2160 inside the parity report
  - **R-11.2** Reactor centre alignment within ±5 px AND ring-radii ratios within ±5 % vs JARVIS.gif extracted frame
  - **R-11.3** 10-second frame-by-frame diff at 30 fps shows ≤ 3 % per-frame Δ in the matched ROI vs reference
- **R-12** Iron Man HUD asset language preserved
  - **R-12.1** Telemetry labels (CPU%, GPU%, MEM%, TEMP, POWER, NET MB/s) present in DOM AND positioned outside the reactor central disk
  - **R-12.2** Mode bar elements (HOME / SEC / DIAG / NET / MEDIA) present and clickable
  - **R-12.3** Calendar dial (`tl-calendar`), power cell (`left-power`), comms dial (`left-comm`) carried forward from V3
  - **R-12.4** Four amber-reason endpoint plates (`amberReasonCpu`, `amberReasonMem`, `amberReasonThermal`, `amberReasonPower`) present with data-driven text
- **R-13** Interactivity preserved + extended
  - **R-13.1** Trigger panel buttons fire correct telemetry side effects (CPU SPIKE, GPU SURGE, THERMAL CRITICAL, POWER SURGE, CHARGE SURGE, MEM PRESSURE, NET BURST, DISK I/O)
  - **R-13.2** Phase transitions (`JARVIS.boot()`, `JARVIS.lock()`, `JARVIS.shutdown()`) trigger camera dolly + bloom envelope + audio cue (where audio context is initialised)
  - **R-13.3** Mouse hover on a telemetry endpoint highlights the connected neural line (stroke colour + width transition)
  - **R-13.4** Keyboard shortcuts (`Space` boot, `Enter` shutdown, `B` battery low, `C` charge, `R` reboot, `L` lock) preserved verbatim from V3
- **R-14** Preserve all existing v2/V3 functional invariants
  - **R-14.1** `getAnimations()` enumeration: post ⊇ pre (no dropped animations from V3)
  - **R-14.2** UHD sharpness preserved at 3840×2160 — no pixelation, no scaling artefacts
  - **R-14.3** Color palette preserved — only authorised deltas: white outer-ring (V3), neural-pulse colour (V3), and one new emissive tone for PBR ring material if required
  - **R-14.4** Zero new external CDN URLs (all new code loads from bundled `node_modules` via Vite output)
- **R-15** Marvel-build parity report deliverable
  - **R-15.1** `docs/jarvis-uhd-cinematic-v2-parity-report.md` updated with a new `§0.7 MARVEL-GRADE BUILD` section (additive; do NOT replace prior V3/V3-iter2 sections)
  - **R-15.2** Side-by-side captures embedded at 1280 / 1920 / 2560 / 3840 viewports
  - **R-15.3** FPS trace, palette diff, animation enumeration diff, and per-test pass/fail with evidence links included
  - **R-15.4** License-compatibility notes present for any GPL/LGPL/AGPL packages in the new dependency surface

## §3 · Success Criteria

- **SC-1.1 (R-1.1):** `Glob prototypes/*.html` returns ≥ 5 files; enumeration recorded in parity report appendix
- **SC-1.2 (R-1.2):** Parity report explicitly names `jarvis-uhd-cinematic-v2.html` as parent AND lists ≥ 5 carried-forward features
- **SC-1.3 (R-1.3):** `curl -I http://localhost:8899/jarvis-reactor-cinematic-marvel.html` returns HTTP 200
- **SC-1.4 (R-1.4):** `curl -I` for v2, compare, drkdna-baseline, reactor-target each returns 200; headless Chrome load reports `console.error` count == 0 for each
- **SC-2.1 (R-2.1):** Code grep finds `new EffectComposer(`, `new RenderPass(`, `new EffectPass(` in `js/src/reactor-cinematic-marvel.ts`
- **SC-2.2 (R-2.2):** Sampled bloom-canvas mean luminance ≥ 1.20 × baseline mean luminance over 5 s capture
- **SC-2.3 (R-2.3):** Code grep finds `ChromaticAberrationEffect` instantiation passed to `EffectPass` constructor
- **SC-2.4 (R-2.4):** Code grep finds anamorphic lens flare GLSL fragment shader chunk (`vec3 flare`) inside a `ShaderPass` registration
- **SC-2.5 (R-2.5):** Sampled god-rays canvas pixel count where `r+g+b > 30` is ≥ 1000
- **SC-3.1 (R-3.1):** Three.js scene introspection (via injected `window.__SCENE`) lists ≥ 3 meshes named `coreRing*` AND code grep confirms cut intervals exactly `[(30,40),(180,190),(210,220)]` and double-ring spans `[(40,170),(230,350)]`
- **SC-3.2 (R-3.2):** 5 s 30 fps capture: `dθ/dt` signs alternate across adjacent layers AND per-layer angular-velocity variance ≥ 15 %
- **SC-3.3 (R-3.3):** Code grep finds `SpotLight` import from `@react-three/drei` (or `three`) configured with `volumetric: true` (or equivalent volumetric pipeline)
- **SC-3.4 (R-3.4):** Material introspection: each `coreRing*` mesh material has `metalness ≥ 0.7` AND `roughness ≤ 0.3` AND a non-null `emissive` map driven by audio bass band
- **SC-4.1 (R-4.1):** Code grep finds `three-particles` import + `RendererType.INSTANCED` usage + a `JT.trigger('charge')` hook that emits a particle burst
- **SC-4.2 (R-4.2):** Particle count sample during NOMINAL phase ≥ 50
- **SC-4.3 (R-4.3):** Programmatic click on each trigger panel button fires the expected mood-engine side effect AND a measurable spark particle count rise within 200 ms
- **SC-5.1 (R-5.1):** SVG DOM inventory: 4 trunk paths + ≥ 20 leaf paths
- **SC-5.2 (R-5.2):** Each leaf path's `<animateMotion>` `dur` attribute parses to ≥ 2 s
- **SC-5.3 (R-5.3):** Computed contrast ratio for every endpoint plate ≥ 4.5 : 1
- **SC-5.4 (R-5.4):** DOM scan: zero text-bearing elements have bbox intersecting disk `(CX, CY, R*0.55)`
- **SC-6.1 (R-6.1):** Code grep finds `Meyda.createMeydaAnalyzer` invocation referencing `MOOD.bpm` (or equivalent clock binding)
- **SC-6.2 (R-6.2):** 5 s sample capture: Pearson correlation between `bloomMul` series and bass-band series ≥ 0.5
- **SC-6.3 (R-6.3):** 5 s sample capture: Pearson correlation between `ringSpeedMul` series and (mid + treble) band series ≥ 0.5
- **SC-6.4 (R-6.4):** Mic-denied test path: zero `console.error` calls; `audioFallback === 'silent'` flag observable on `window.JARVIS.audio`
- **SC-7.1 (R-7.1):** Code grep finds `gsap.registerPlugin(MotionPathPlugin)` AND ≥ 1 `motionPath:` call inside boot/lock/shutdown handlers
- **SC-7.2 (R-7.2):** Headless capture during BOOT phase shows SVG path stroke-dasharray reveal animation between t=0 and t=2s
- **SC-8.1 (R-8.1):** Code grep finds `PerspectiveCamera` instantiation AND a phase-transition handler that mutates `camera.position.z` (dolly) within an easing tween
- **SC-8.2 (R-8.2):** Code grep finds exactly one `requestAnimationFrame(frame)` driver in the new module; zero stray `setInterval` calls used for animation cadence
- **SC-8.3 (R-8.3):** Chrome DevTools performance trace over 30 s reports sustained ≥ 58 FPS at 1920×1080
- **SC-9.1 (R-9.1):** `uv sync --frozen` exits 0 with no resolution changes
- **SC-9.2 (R-9.2):** `npm ci --prefix js` exits 0 with zero peer-dep warnings
- **SC-9.3 (R-9.3):** `js/src/reactor-cinematic-marvel.ts` exists; all `import` statements resolve to `node_modules` paths (no inline URLs)
- **SC-9.4 (R-9.4):** `npm run build --prefix js` produces `prototypes/dist/reactor-cinematic-marvel.js` (size > 0)
- **SC-9.5 (R-9.5):** Headless Chrome network panel reports zero 404s when loading `prototypes/jarvis-reactor-cinematic-marvel.html`
- **SC-10.1 (R-10.1):** `compare.html` DOM inspection: a new chip element exists for the marvel baseline AND clicking it swaps the comparator iframe `src`
- **SC-10.2 (R-10.2):** Capture at 1920×1080 with comparator centre crosshair visible: crosshair intersects new baseline reactor centre within ±2 px (image-coordinate measurement)
- **SC-11.1 (R-11.1):** Captures saved at `docs/jarvis-uhd-cinematic-v2-parity/marvel/marvel-{1920,3840}.png`
- **SC-11.2 (R-11.2):** Image-coordinate measurement vs `docs/jarvis-uhd-cinematic-v2-parity/jarvis-gif-frame.png`: centre alignment within ±5 px AND ring radii ratios within ±5 %
- **SC-11.3 (R-11.3):** 10 s frame-by-frame diff vs reference video frame sequence in matched ROI: ≤ 3 % per-frame Δ
- **SC-12.1 (R-12.1):** DOM scan: 6 telemetry label elements present (`cpuPct`, `gpuPct`, `memPct`, `tempC`, `powerW`, `netRate`) AND each bbox is outside disk `(CX, CY, R*0.55)`
- **SC-12.2 (R-12.2):** DOM scan: 5 mode-bar elements present matching `[HOME, SEC, DIAG, NET, MEDIA]`
- **SC-12.3 (R-12.3):** DOM scan: `.tl-calendar`, `.left-power`, `.left-comm` elements present
- **SC-12.4 (R-12.4):** DOM scan: 4 elements with id matching `amberReason*` present AND each `textContent` is non-empty AND non-placeholder
- **SC-13.1 (R-13.1):** For each of 8 trigger button programmatic clicks, the corresponding `TEL.*` field changes within 200 ms
- **SC-13.2 (R-13.2):** Calling `window.JARVIS.boot()` headlessly: camera `position.z` mutates AND `bloomMul` rises from 0 to ≥ 0.8 within 10 s
- **SC-13.3 (R-13.3):** Programmatic `mouseover` on an endpoint causes the connected neural-line element's computed `stroke` width to increase by ≥ 0.5 px
- **SC-13.4 (R-13.4):** Keyboard event dispatch for each of `Space`, `Enter`, `B`, `C`, `R`, `L` triggers the expected V3 handler
- **SC-14.1 (R-14.1):** `getAnimations()` enumeration diff between V3 `jarvis-uhd-cinematic-v2.html` and the marvel baseline: marvel set ⊇ V3 set
- **SC-14.2 (R-14.2):** Render at 3840×2160: visual inspection PASS (no pixelation, no scaling artefacts)
- **SC-14.3 (R-14.3):** Color palette extraction: `palette_marvel ⊆ palette_v3 ∪ {white, neural-pulse, marvel-emissive-tone}`
- **SC-14.4 (R-14.4):** Diff scan of marvel HTML + bundled JS: zero new external CDN URLs in `<script src=>`, `<link href=>`, `@import`, CSS `url(...)` (existing Google Fonts in v2 baseline permitted)
- **SC-15.1 (R-15.1):** Parity report contains a new `§0.7` heading with marvel-build content
- **SC-15.2 (R-15.2):** Parity report embeds captures at 1280 / 1920 / 2560 / 3840 viewports
- **SC-15.3 (R-15.3):** Parity report contains FPS trace JSON, palette diff table, animation enumeration table, and a per-test pass/fail row
- **SC-15.4 (R-15.4):** License-compatibility table in parity report lists every GPL/LGPL/AGPL dep and its compatibility status

## §4 · Constraints & Validation Gates

- **HC-1:** Existing V3 colour palette MUST be preserved verbatim; only authorised deltas are the V3 white outer-ring stroke, the V3 neural-pulse colour, and one new `--marvel-emissive-tone` if PBR ring emissive requires it
- **HC-2:** Reactor central disk `(CX, CY, R*0.55)` MUST contain zero text-bearing element bounding boxes (V3 invariant carried forward)
- **HC-3:** New 3D C-shape rings MUST visually dominate the inner reactor — drawn pixel area exceeds any other ring set in the same frame
- **HC-4:** No new external CDN URLs introduced; all new code loads from bundled `node_modules` via Vite output
- **HC-5:** No vendoring of LGPL-2.1 or otherwise incompatibly-licensed code/assets from `Jarvis-Desktop`; visual-reference inspection only
- **HC-6:** Local servers `:8787` and `:8899` MUST be reachable and serving HTTP 200 during validation
- **HC-7:** `/Users/vic/Downloads/JARVIS.gif` MUST exist and be readable
- **HC-8:** All existing prototypes (`compare.html`, `jarvis-uhd-cinematic-v2.html`, `jarvis-drkdna-baseline.html`, `jarvis-reactor-target.html`) MUST remain functional after the change (zero regressions)
- **HC-9:** `uv.lock` and `js/package-lock.json` from the immediately-preceding `/prompt-reconstruct` step are AUTHORITATIVE; `uv sync --frozen` and `npm ci --prefix js` MUST both exit 0 with zero drift
- **HC-10:** Sustained ≥ 58 FPS at 1920×1080 over a 30 s Chrome DevTools performance trace
- **HC-11:** Marvel-grade quality bar — bloom AND chromatic aberration AND anamorphic lens flare AND god rays MUST all be visible and contributing measurable pixels in a sampled frame
- **HC-12:** Production-grade only — zero placeholder strings (`TODO`, `FIXME`, `PLACEHOLDER`, `MOCK`, `XXX`, `lorem`) in new code
- **C-1:** All new keyframe animations use `transform` and `opacity` only (GPU-accelerated; no `top`/`left`/`width`/`height` keyframes)
- **C-2:** New 3D ring geometry rendered via Three.js + WebGL only (no Canvas 2D for new ring meshes); legacy V3 Canvas-2D layers may remain for backwards compatibility but the marvel C-rings are WebGL
- **C-3:** All animation cadence references the single `MOOD.bpm` clock; no parallel `setInterval` driver for animation
- **C-4:** WCAG AA contrast (≥ 4.5 : 1) for all repositioned telemetry text
- **C-5:** Single Vite build pipeline; no parallel build systems or hand-edited bundle output
- **C-6:** JARVIS.gif and the four Vecteezy references are canonical visual fidelity targets; deviations require explicit user approval (VG-7)
- **VG-1 (gates S-1; verifies HC-6, HC-7, HC-9):** `curl -I http://localhost:8787/index.html` AND `curl -I http://localhost:8899/jarvis-uhd-cinematic-v2.html` AND `curl -I http://localhost:8899/compare.html` all return 200 AND `test -s /Users/vic/Downloads/JARVIS.gif` exits 0; HALT if any fails
- **VG-2 (gates S-1; verifies HC-9):** `uv sync --frozen` AND `npm ci --prefix js` both exit 0 with zero drift; HALT if either reports lockfile changes
- **VG-3 (gates S-5; verifies HC-11, R-2):** All four cinematic effect canvases (bloom, chromatic aberration, anamorphic flare, god rays) contribute non-zero pixels in a sampled frame; HALT if any effect shows zero contribution
- **VG-4 (gates S-7; verifies HC-2, HC-3):** DOM scan returns zero text bbox intersecting `(CX, CY, R*0.55)` AND C-ring drawn pixel area exceeds any other ring set; HALT on either violation
- **VG-5 (gates S-10; verifies R-14.1, HC-4):** `getAnimations()` enumeration diff is empty AND zero new external CDN URLs detected; HALT on regression
- **VG-6 (gates S-10; verifies HC-10):** Chrome DevTools performance trace ≥ 58 FPS sustained over 30 s; HALT on FPS drop below threshold
- **VG-7 (gates S-11; verifies R-11, C-6):** Side-by-side visual diff of marvel baseline + recordings + reference frames presented to the user via `compare.html`; HARD HALT until explicit user approval
- **VG-8 (gates S-10; verifies HC-1, HC-12):** Palette extraction confirms `palette_marvel ⊆ palette_v3 ∪ {authorised}` AND code-quality grep returns zero placeholder strings; HALT on either violation

## §5 · Test Plan

- **T-1 (R-1.1, SC-1.1):** Use the `Glob` tool with pattern `prototypes/*.html`; assert returned list contains ≥ 5 files; record JSON in parity report
- **T-2 (R-1.2, SC-1.2):** Use the `Read` tool to load the parity report; assert `jarvis-uhd-cinematic-v2.html` named as parent AND ≥ 5 carried-forward features enumerated
- **T-3 (R-1.3, SC-1.3):** `curl -I http://localhost:8899/jarvis-reactor-cinematic-marvel.html`; assert HTTP 200
- **T-4 (R-1.4, SC-1.4, HC-8):** `curl -I` for each existing prototype + headless Chrome load each via `cursor-ide-browser` `browser_navigate`; assert HTTP 200 AND `console.error` count == 0
- **T-5 (R-2.1, SC-2.1):** Code grep on `js/src/reactor-cinematic-marvel.ts` for `new EffectComposer(`, `new RenderPass(`, `new EffectPass(`; all three present
- **T-6 (R-2.2, SC-2.2):** Headless Chrome capture via `cursor-ide-browser` `browser_take_screenshot`; sample bloom region mean luminance via Pillow; assert ≥ 1.20× baseline (where baseline is a screenshot taken with `?nobloom=1` query param)
- **T-7 (R-2.3, SC-2.3):** Code grep for `ChromaticAberrationEffect` in `EffectPass(...)` constructor argument list
- **T-8 (R-2.4, SC-2.4):** Code grep for anamorphic lens flare GLSL chunk (`vec3 flare`) inside `ShaderPass` or `ShaderMaterial` registration
- **T-9 (R-2.5, SC-2.5):** Inject Pillow-based pixel sampler over god-rays canvas region; assert non-zero pixel count ≥ 1000
- **T-10 (R-3.1, SC-3.1):** Inject `window.__SCENE = scene` debug hook; query for `coreRing*` named meshes; assert ≥ 3 AND assert cut intervals + double-ring spans match prior spec
- **T-11 (R-3.2, SC-3.2):** 5 s capture at 30 fps; per-frame angular phase extraction (Hough); assert alternating `dθ/dt` signs across adjacent layers AND velocity variance ≥ 15 %
- **T-12 (R-3.3, SC-3.3):** Code grep for `SpotLight` import from `@react-three/drei` (or `three`) configured with `volumetric: true` (or `VolumetricSpotLight`); assert present
- **T-13 (R-3.4, SC-3.4):** `window.__SCENE` introspection: each `coreRing*` mesh material has `metalness ≥ 0.7`, `roughness ≤ 0.3`, AND a non-null `emissive` channel
- **T-14 (R-4.1, SC-4.1):** Code grep for `three-particles` import AND `RendererType.INSTANCED` AND a `JT.trigger('charge')` hook emitting particles
- **T-15 (R-4.2, SC-4.2):** During NOMINAL phase, sample `window.__PARTICLES.count`; assert ≥ 50
- **T-16 (R-4.3, SC-4.3):** Programmatically click each of 8 trigger buttons; assert corresponding `TEL.*` field mutation within 200 ms AND particle count rise after charge
- **T-17 (R-5.1, SC-5.1):** SVG DOM inventory; assert 4 trunks + ≥ 20 leaves
- **T-18 (R-5.2, SC-5.2):** Inspect every leaf's `<animateMotion>` `dur` attribute; assert each parses to ≥ 2 s
- **T-19 (R-5.3, SC-5.3):** For each endpoint plate, compute WCAG AA contrast ratio via Pillow + colour-parser; assert ≥ 4.5 : 1
- **T-20 (R-5.4, SC-5.4, HC-2):** DOM scan; for every text-bearing element compute bbox; assert none intersect disk `(CX, CY, R*0.55)`
- **T-21 (R-6.1, SC-6.1):** Code grep for `Meyda.createMeydaAnalyzer` referencing `MOOD.bpm` clock binding
- **T-22 (R-6.2, SC-6.2):** Headless capture with mock audio source playing a low-frequency tone; sample `bloomMul` and bass-band series for 5 s; compute Pearson correlation; assert ≥ 0.5
- **T-23 (R-6.3, SC-6.3):** Same capture; sample `ringSpeedMul` and (mid + treble) series for 5 s; assert Pearson ≥ 0.5
- **T-24 (R-6.4, SC-6.4):** Headless capture with `permissions: { microphone: 'denied' }`; assert zero `console.error` AND `window.JARVIS.audio.fallback === 'silent'`
- **T-25 (R-7.1, SC-7.1):** Code grep for `gsap.registerPlugin(MotionPathPlugin)` AND ≥ 1 `motionPath:` reference inside `boot|lock|shutdown` handler functions
- **T-26 (R-7.2, SC-7.2):** Headless capture frames every 100 ms during BOOT phase for 2 s; assert SVG path `stroke-dasharray` value changes between consecutive frames
- **T-27 (R-8.1, SC-8.1):** Code grep for `PerspectiveCamera` AND phase-transition handler mutating `camera.position.z` inside a tween
- **T-28 (R-8.2, SC-8.2):** Code grep: exactly one `requestAnimationFrame(frame)` AND zero `setInterval` calls used for animation cadence (telemetry-update setInterval allowed)
- **T-29 (R-8.3, SC-8.3, HC-10):** Chrome DevTools performance trace via `plugin-chrome-devtools-mcp-chrome-devtools` MCP `performance_start_trace`/`performance_stop_trace` over 30 s at 1920×1080; assert sustained ≥ 58 FPS
- **T-30 (R-9.1, SC-9.1, HC-9):** `uv sync --frozen` via Shell; assert exit 0 AND no lockfile mutation
- **T-31 (R-9.2, SC-9.2, HC-9):** `npm ci --prefix js` via Shell; assert exit 0 AND zero peer-dep warnings
- **T-32 (R-9.3, SC-9.3):** Use `Read` tool on `js/src/reactor-cinematic-marvel.ts`; assert all `import` statements resolve to bare module specifiers (no inline URLs)
- **T-33 (R-9.4, SC-9.4):** `npm run build --prefix js` via Shell; assert `prototypes/dist/reactor-cinematic-marvel.js` exists with size > 0
- **T-34 (R-9.5, SC-9.5):** `cursor-ide-browser` `browser_navigate` to the marvel HTML; query the network panel; assert zero 404s
- **T-35 (R-10.1, SC-10.1):** `cursor-ide-browser` `browser_snapshot` of `compare.html`; assert new chip element present AND clicking it mutates the comparator iframe `src`
- **T-36 (R-10.2, SC-10.2):** Capture at 1920×1080 with crosshair visible; image-coordinate measurement: crosshair intersects new baseline reactor centre within ±2 px
- **T-37 (R-11.1, SC-11.1):** Save captures to `docs/jarvis-uhd-cinematic-v2-parity/marvel/marvel-{1920,3840}.png`; assert files present and non-empty
- **T-38 (R-11.2, SC-11.2):** Pillow-based image comparison vs `docs/jarvis-uhd-cinematic-v2-parity/jarvis-gif-frame.png`; assert centre alignment ±5 px AND ring radii ratios ±5 %
- **T-39 (R-11.3, SC-11.3):** 10 s synchronised frame capture vs reference; per-frame diff in matched ROI; assert ≤ 3 % per-frame Δ
- **T-40 (R-12.1, SC-12.1):** DOM scan; assert 6 telemetry label elements (`cpuPct`, `gpuPct`, `memPct`, `tempC`, `powerW`, `netRate`) present AND each bbox outside disk `(CX, CY, R*0.55)`
- **T-41 (R-12.2, SC-12.2):** DOM scan; assert 5 mode-bar elements with text matching `[HOME, SEC, DIAG, NET, MEDIA]`
- **T-42 (R-12.3, SC-12.3):** DOM scan; assert `.tl-calendar`, `.left-power`, `.left-comm` elements present
- **T-43 (R-12.4, SC-12.4):** DOM scan; assert 4 `amberReason*` elements present AND each `textContent` non-empty AND non-placeholder
- **T-44 (R-13.1, SC-13.1):** For each of 8 trigger buttons, programmatic click; assert corresponding `TEL.*` field mutation within 200 ms
- **T-45 (R-13.2, SC-13.2):** Headless invocation `window.JARVIS.boot()`; sample camera `position.z` and `bloomMul` over 10 s; assert camera mutation AND bloomMul rises from 0 to ≥ 0.8
- **T-46 (R-13.3, SC-13.3):** Programmatic `mouseover` on an endpoint; assert connected neural-line element's computed `stroke-width` increases by ≥ 0.5 px
- **T-47 (R-13.4, SC-13.4):** Dispatch keyboard events for `Space`, `Enter`, `B`, `C`, `R`, `L`; assert each triggers the expected V3 handler (verified via `STATE.phase` mutation or equivalent)
- **T-48 (R-14.1, SC-14.1):** Enumerate `getAnimations()` for V3 baseline AND for marvel baseline; assert marvel set ⊇ V3 set
- **T-49 (R-14.2, SC-14.2):** Render marvel baseline at 3840×2160 via `cursor-ide-browser` `browser_take_screenshot`; visual inspection PASS (no pixelation, no scaling artefacts)
- **T-50 (R-14.3, SC-14.3, HC-1):** Color palette extraction on a marvel-baseline frame; assert `palette_marvel ⊆ palette_v3 ∪ {white, neural-pulse, marvel-emissive-tone}`
- **T-51 (R-14.4, SC-14.4, HC-4):** Diff scan of marvel HTML + bundled JS; assert zero new external CDN URLs in `<script src=>`, `<link href=>`, `@import`, CSS `url(...)`
- **T-52 (R-15.1, SC-15.1):** Use `Read` tool on `docs/jarvis-uhd-cinematic-v2-parity-report.md`; assert new `§0.7 MARVEL-GRADE BUILD` section present
- **T-53 (R-15.2, SC-15.2):** Assert parity report embeds image references for captures at 1280 / 1920 / 2560 / 3840 viewports
- **T-54 (R-15.3, SC-15.3):** Assert parity report contains FPS trace JSON, palette diff table, animation enumeration table, and per-test pass/fail row
- **T-55 (R-15.4, SC-15.4, HC-5):** Assert parity report's license-compatibility table lists every GPL/LGPL/AGPL dep with compatibility status
- **T-56 (HC-3):** Pixel-area count via Pillow on a marvel-baseline frame: C-ring drawn pixel area exceeds any other ring set in the same frame
- **T-57 (HC-6, VG-1):** `curl -I http://localhost:8787/index.html` AND `curl -I http://localhost:8899/jarvis-uhd-cinematic-v2.html` AND `curl -I http://localhost:8899/compare.html`; assert all 200
- **T-58 (HC-7, VG-1):** `test -s /Users/vic/Downloads/JARVIS.gif`; assert exit 0
- **T-59 (HC-11):** Capture annotated screenshot showing bloom + chromatic aberration + anamorphic lens flare + god rays all visibly contributing; embed in parity report
- **T-60 (HC-12):** Code grep on `js/src/reactor-cinematic-marvel.ts` AND `prototypes/jarvis-reactor-cinematic-marvel.html` for placeholder strings (`TODO`, `FIXME`, `PLACEHOLDER`, `MOCK`, `XXX`, `lorem`); assert zero matches

## §6 · Deliverables Map

- **D-1 (R-1.1, R-1.2, R-1.3, R-1.4, R-9.3, R-9.4, R-9.5, R-13.1, R-13.2, R-13.3, R-13.4, SC-1.1, SC-1.2, SC-1.3, SC-1.4, SC-9.3, SC-9.4, SC-9.5, SC-13.1, SC-13.2, SC-13.3, SC-13.4, T-1, T-2, T-3, T-4, T-32, T-33, T-34, T-44, T-45, T-46, T-47):** New marvel-baseline files: `js/src/reactor-cinematic-marvel.ts` (entry point) + `prototypes/jarvis-reactor-cinematic-marvel.html` (HTML loader) + `prototypes/dist/reactor-cinematic-marvel.js` (Vite build output) + carried-forward V3 trigger panel and keyboard shortcuts wired into the new pipeline
- **D-2 (R-2.1, R-2.2, R-2.3, R-2.4, R-2.5, R-3.1, R-3.2, R-3.3, R-3.4, R-7.1, R-7.2, R-8.1, R-8.2, R-8.3, SC-2.1, SC-2.2, SC-2.3, SC-2.4, SC-2.5, SC-3.1, SC-3.2, SC-3.3, SC-3.4, SC-7.1, SC-7.2, SC-8.1, SC-8.2, SC-8.3, T-5, T-6, T-7, T-8, T-9, T-10, T-11, T-12, T-13, T-25, T-26, T-27, T-28, T-29):** Three.js scene module + `pmndrs/postprocessing` `EffectComposer` pipeline (bloom, chromatic aberration, anamorphic flare ShaderPass, god rays Effect) + PBR C-ring meshes + drei volumetric SpotLight + GSAP MotionPath choreography + Anime.js boot-stroke draw + PerspectiveCamera dolly engine — all inside `js/src/reactor-cinematic-marvel.ts` modules
- **D-3 (R-4.1, R-4.2, R-4.3, R-6.1, R-6.2, R-6.3, R-6.4, SC-4.1, SC-4.2, SC-4.3, SC-6.1, SC-6.2, SC-6.3, SC-6.4, T-14, T-15, T-16, T-21, T-22, T-23, T-24):** `three-particles` GPU-instanced particle module + Meyda audio-reactive engine bound to MOOD.bpm with bass→bloom and (mid+treble)→ringSpeed correlations + mic-denied silent fallback path
- **D-4 (R-5.1, R-5.2, R-5.3, R-5.4, R-12.1, R-12.2, R-12.3, R-12.4, SC-5.1, SC-5.2, SC-5.3, SC-5.4, SC-12.1, SC-12.2, SC-12.3, SC-12.4, T-17, T-18, T-19, T-20, T-40, T-41, T-42, T-43):** SVG dendritic neural pathway layer (4 trunks + ≥ 20 leaves with `<animateMotion>` pulses) + endpoint plates + telemetry HUD elements (calendar dial, power cell, comms dial, mode bar, amber-reason plates) carried forward and integrated into the new pipeline
- **D-5 (R-10.1, R-10.2, SC-10.1, SC-10.2, T-35, T-36):** Additive edits to `prototypes/compare.html` registering the marvel baseline as a selectable chip + crosshair calibration
- **D-6 (R-11.1, R-11.2, R-11.3, R-14.1, R-14.2, R-14.3, R-14.4, R-15.1, R-15.2, R-15.3, R-15.4, SC-11.1, SC-11.2, SC-11.3, SC-14.1, SC-14.2, SC-14.3, SC-14.4, SC-15.1, SC-15.2, SC-15.3, SC-15.4, T-37, T-38, T-39, T-48, T-49, T-50, T-51, T-52, T-53, T-54, T-55, T-56, T-59):** Updated `docs/jarvis-uhd-cinematic-v2-parity-report.md` with new `§0.7 MARVEL-GRADE BUILD` section containing screenshots at 4 viewports, FPS trace JSON, palette diff table, animation enumeration diff, license-compatibility table, and the HC-11 cinematic-effects-visible annotated screenshot
- **D-7 (R-9.1, R-9.2, SC-9.1, SC-9.2, T-30, T-31):** Lockfile freshness verification log at `docs/smoke-results/marvel-build-lockfile-verify.log` confirming `uv sync --frozen` and `npm ci --prefix js` both exit 0 with zero drift
- **D-8 (HC-12, T-60):** Code-quality grep report at `docs/smoke-results/marvel-build-code-quality.log` confirming zero placeholder strings in new code

## §7 · Quality Standards

- TypeScript 5+ with `strict: true`, `noUncheckedIndexedAccess: true`; zero `any` types in new code
- Vite 6+ build pipeline; `build.target: 'esnext'`, `build.minify: true`, `build.sourcemap: true` for production bundles
- ESM-only — no CommonJS `require()` in new code
- All animations use `transform` and `opacity` only (C-1); no layout-thrashing properties in keyframes
- 3D ring geometry uses `MeshPhysicalMaterial` (PBR); legacy V3 Canvas-2D rings preserved as a backwards-compat layer; the marvel C-rings are exclusively WebGL (C-2)
- Single `requestAnimationFrame` driver; `MOOD.bpm` is the sole animation clock (C-3); telemetry update `setInterval` (1 Hz) is the only non-rAF timer permitted
- WCAG AA contrast (≥ 4.5 : 1) for all repositioned telemetry text (C-4)
- All cadence wired through `MOOD.bpm`; no parallel uncontrolled animation timers
- No console errors at runtime in Chrome DevTools (verified during T-29)
- HTML validates per W3C Markup Validator
- All new renderer functions documented with header comments explaining the dependency they wrap (postprocessing, three-particles, Meyda, GSAP MotionPath)
- Color palette centralised in CSS custom properties; zero stray hex literals (T-50 + palette extraction)
- Source files formatted via Prettier default config; lint clean via ESLint with `@typescript-eslint/recommended-strict`
- Parity report (D-6) follows reproducible structure: assertion → command/method → evidence (image / JSON / trace) → pass/fail per test
- License-compatibility table in parity report lists every GPL/LGPL/AGPL dep AND its compatibility status; `Jarvis-Desktop` LGPL-2.1 noted as inspection-only
- A line is appended to `docs/remediation-checklist.md` documenting the V4 marvel-build addition with date and one-line description
- Zero placeholder strings (`TODO`, `FIXME`, `PLACEHOLDER`, `MOCK`, `XXX`, `lorem`) in new code (T-60)

## §8 · Execution Order

- **S-1 (advances R-1, R-9; gates VG-1, VG-2; produces D-7):** Verify all system prereqs and lockfile freshness — run `curl -I http://localhost:8787/index.html`, `curl -I http://localhost:8899/jarvis-uhd-cinematic-v2.html`, `curl -I http://localhost:8899/compare.html`, `test -s /Users/vic/Downloads/JARVIS.gif`, `uv sync --frozen`, AND `npm ci --prefix js` via the `Shell` tool; HALT on any non-success. Use the `Glob` tool with pattern `prototypes/**/*.html` to inventory existing prototypes; use the `Read` tool to load `prototypes/jarvis-uhd-cinematic-v2.html`, `prototypes/compare.html`, `prototypes/jarvis-drkdna-baseline.html`, `prototypes/jarvis-reactor-target.html` (NEVER `cat` / `head` / `tail`). Write lockfile verification log (D-7).
- **S-2 (advances R-1.1, R-1.2; produces D-1 partial):** Document the prototype inventory and the carried-forward feature list (MOOD/BPM engine, neural pathways, telemetry layout, trigger panel, phase state machine, JARVIS public API) in a fresh `§0.7` heading scaffolded inside `docs/jarvis-uhd-cinematic-v2-parity-report.md` via `StrReplace` (NEVER `sed` / `awk`).
- **S-3 (advances R-9.3, R-9.5, R-12; produces D-1 + D-4 partial):** Use the `Write` tool to create `js/src/reactor-cinematic-marvel.ts` (entry point, imports only from locked `node_modules`) AND `prototypes/jarvis-reactor-cinematic-marvel.html` (loader that mounts the bundle output + carries forward all V3 telemetry HUD elements: calendar dial, power cell, comms dial, mode bar, amber-reason plates, telemetry labels positioned outside the central reactor disk).
- **S-4 (advances R-3, R-8; produces D-2 partial):** Implement the Three.js scene inside `js/src/reactor-cinematic-marvel.ts`: PerspectiveCamera with parallax + dolly tween wired to `setPhase()`, ≥ 3 named C-shape ring meshes (`coreRingInner`, `coreRingMid`, `coreRingOuter`) with the V3 cut + double-ring spec preserved exactly, counter-rotation via non-linear easing, drei volumetric `SpotLight` illumination, `MeshPhysicalMaterial` with `metalness ≥ 0.7` + `roughness ≤ 0.3` + audio-bass-driven `emissive`. Edit via `StrReplace` for any subsequent in-file refinement.
- **S-5 (advances R-2, R-7; gates VG-3; produces D-2 final):** Wire `pmndrs/postprocessing` `EffectComposer` with `RenderPass` + `BloomEffect` (HDR tone mapping) + `ChromaticAberrationEffect` + ported anamorphic lens flare `ShaderPass` (from `Anamorphic Lens Flare` Godot shader, MIT) + ported god rays `Effect` (from `Erkaman/glsl-godrays`); register `gsap.registerPlugin(MotionPathPlugin)` and choreograph BOOT/LOCK/SHUTDOWN sequences; wire Anime.js (or Motion One) for SVG path stroke draw on BOOT entry; HALT on VG-3 violation (any of the 4 cinematic effect canvases contributing zero pixels).
- **S-6 (advances R-4, R-6; produces D-3):** Wire `three-particles` GPU-instanced spark renderer for charge-event bursts (hooked into `JT.trigger('charge')`); wire ambient motes during NOMINAL phase; wire `Meyda.createMeydaAnalyzer` bound to `MOOD.bpm` with bass→`bloomMul` and (mid+treble)→`ringSpeedMul`; implement mic-denied silent fallback path (zero console errors).
- **S-7 (advances R-5, R-12; gates VG-4; produces D-4 final):** Carry forward and integrate the V3 SVG dendritic neural pathway layer (4 trunks + ≥ 20 leaves) into the new bundle; preserve `<animateMotion>` pulse animations (≥ 2 s per traversal); ensure WCAG AA contrast on every endpoint plate; verify zero text bbox intersects disk `(CX, CY, R*0.55)` AND C-ring drawn pixel area exceeds any other ring set; HALT on VG-4 violation.
- **S-8 (advances R-13; produces D-1 final):** Wire trigger panel buttons (CPU SPIKE, GPU SURGE, THERMAL, POWER SURGE, CHARGE SURGE, MEM PRESSURE, NET BURST, DISK I/O) to fire correct telemetry side effects + new GPU spark cascade + new bloom envelope; wire phase transitions (`JARVIS.boot()`, `JARVIS.lock()`, `JARVIS.shutdown()`) to camera dolly + bloom envelope + audio cue; wire endpoint hover to connected-neural-line stroke transition; preserve V3 keyboard shortcuts (`Space`, `Enter`, `B`, `C`, `R`, `L`).
- **S-9 (advances R-9.4, R-10; produces D-5):** Run `npm run build --prefix js` via Shell to produce `prototypes/dist/reactor-cinematic-marvel.js`; update `prototypes/compare.html` via `StrReplace` to register the marvel baseline as a selectable chip in the chip-bar AND ensure centre crosshairs align with the marvel baseline's reactor centre; validate via T-35 + T-36.
- **S-10 (advances R-11, R-14; gates VG-5, VG-6, VG-8; produces D-6 partial + D-8):** Run the full T-5 through T-56 + T-59 + T-60 verification battery via `cursor-ide-browser` (`browser_navigate`, `browser_snapshot`, `browser_take_screenshot`) and `plugin-chrome-devtools-mcp-chrome-devtools` MCP (`performance_start_trace` / `performance_stop_trace`) and Pillow-based image analysis via `uv run python`; HALT on any animation regression (VG-5), FPS < 58 sustained (VG-6), or palette/placeholder violation (VG-8); write code-quality log (D-8).
- **S-11 (advances R-15; gates VG-7; produces D-6 final):** Generate the parity report `§0.7 MARVEL-GRADE BUILD` section with screenshots at 1280 / 1920 / 2560 / 3840 viewports, FPS trace JSON, palette diff table, animation enumeration diff, license-compatibility table, and the HC-11 cinematic-effects-visible annotated screenshot; present the rendered marvel baseline + the parity report side-by-side with the references via `compare.html`; HARD HALT until explicit user approval is received.
- **S-12 (gates final commit):** On user approval, append a line to `docs/remediation-checklist.md` documenting the V4 marvel-build addition; commit with a clear message; do NOT push unless the user explicitly requests it.